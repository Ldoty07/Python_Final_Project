The part that was hardest for me was figuring out how to create the classes and all of the methods for them. Before this class, I did not have much experience coding. I had no experience before starting this degree. I had learned a bit of Python in a class in the fall, but we did not go over classes. I was still pretty unfamiliar with them, so it took some time and thought to figure out how to do what I needed to do.

Surprisingly, there were not many bugs in my program. I did not test it until it was nearing completion, which in hindsight is not a good idea, but when I did finally begin testing, the program did not throw as many errors as I was expecting. There were a few times that the program threw an error, such as when I forgot to pass arguments to functions, but they were small and fairly easy to fix. The biggest bug in my code was the JSON not working. I had no experience coding in JavaScript before this semester, and no experience working with JavaScript in Python. I had to go online to figure out what I had to do, and it worked initially, but I only tested it with the products and vendors. Later, when I tested it with the purchase orders, it threw an error saying that the datetime object I was using for the date created attribute is not JSON serializable. I had to convert the datetime object into iso format and then convert it back from iso format to a datetime object. When I first did it, the program threw another error, but I had imported the module datetime instead of the class, so I had to clarify that it was the datetime class's method, not the datetime module.

I had five files. The main one ran the program and had all of the menus and loops. The models file had all of the class definitions and methods for the products, vendors, and purchase orders. The inventory manager file had the functions for dealing with purchase orders. The file manager file had functions for saving and loading the data. The reports file had functions for creating and exporting different reports to and Excel file.

I created a method inside each of  the classes that would convert the class attributes and data into a dictionary so that the data would be able to be saved as JSON data. Whenever the user exits the program, it will run through all of the products, vendors, and purchase orders, convert them into dictionaries, and put them in a list. Then it will write the list of dictionaries to a .json file to be saved. When the program starts, the first thing it will do is load all of the saved data from the .json files. It will read the list of dictionaries from the file and convert the dictionaries back into class objects.

I would try to improve the structure of the code if I had more time. I tried to break as much as I could up, but some blocks of code could probably be put into functions to make the code a little cleaner. I would also have someone give me advice on the formatting of the data printed on the screen. I am not a designer and have a hard time telling if something looks good. I just want it to work as it is intended to. I tried to make the data clear and readable, but I would definitely have someone give me feedback on what could be improved.